# 審査員5人レビュー：改修バックログ

更新: 2026-08-17 ／ 対象: mleague-tracker.html, street-scorer.html, golf-tour.html, index.html

## 済（このセッションで再現→修正→テスト green）
- [x] 色が並び順依存でメンバー削除時にズレる（street）→ 色を本人に固定
- [x] XSSでGitHubトークン窃取が成立（street チップ/ルール/祝儀の属性補間）→ 全て esc()
- [x] 丸め誤差でptが湧く（持ち点モード、実測+0.2pt）→ calcGameで残差を1位に寄せゼロサム化
- [x] ルール欄を空にすると0が入り全履歴が再計算（+40→+140）→ num()/chip欄の空欄ガード
- [x] 取り消しボタンが押せない（#toast pointer-events:none 継承）→ .toast-undoにauto、折返し許可
- [x] 「-10」と打つと+10で保存され自動計算が誤差吸収→無警告誤記録 → 入力の-を符号ボタンに反映
- [x] ご祝儀だけの日が「空」判定で日付上書き/金庫で無確認消去 → newDay・localEmptyにex追加
- [x] 共有リンク取り込みが即push（古いリンクで全消去）→ importSharedをpersist(true)化
- [x] exPreがstate差し替えで stale→例外 → renderEx冒頭でガード＋差し替え時に-1
- [x] fmtPt(NaN)が"0.0"に化ける → isFiniteで"—"
- [x] グラフ終端が順位表とペナルティ分ズレる（▲101.3 vs ▲121.3）→ 調整分を破線で反映、全員一致確認
- [x] グラフ磨き：ゼロ線強調・グリッド破線・y軸▲表記・pt単位・終端に実数（±で緑赤）
- [x] street 6人目の色が緑（+ptと紛らわしい）→ ティール

## 色の方針（代表決定）
- 墨一色にはしない。色は「人を追う場所」（グラフ線・座席牌・順位ドット）＋±の緑赤に限定
- 順位で色統一は却下（数字と重複、かつグラフ線に塗れない）
- ブランドの緑（フェルト＝麻雀卓）は維持。審査員Aの「accent緑をやめろ」は実害小として却下
- 金（お金・精算）機能は作らない（代表意向）
- 9人目以降：色8枠を超えたら「形」を足す案（●○■◆で5色×4形=20人）は保留

## P0（データ・金庫の安全）— street / mleague / golf 完了
- [x] 過去日のルール凍結：day.rules スナップショット。既存日は移行時に現ルールを焼き込むので見た目は不変。ルール編集は「記録中の日」だけ追従
- [x] GitHub保存の競合：syncedT でリモート世代を追跡し、先行していたら push 中止＋通知。409の盲目リトライを撤去
- [x] localStorage保存失敗の可視化：赤バナー常設＋保存トーストが「保存できていません」に変わる
- [x] 敵対的データの無害化：sanitize() で数値/ID/名前を強制。存在しないid宛の祝儀・チップ・対局を除去、nextId修復
- [x] mleague/golf の共有ハッシュ無言適用を停止（未来tでの無確認置換）。tは「いつのデータか」の表示専用にし、
      取り込む/押すの判断からは時計を外した（中身の一致 + 内容ハッシュで判定）
- [x] mleague に4点を移植（ルール凍結は不要／競合ガード・保存失敗・sanitize を実装）
- [x] golf に sanitize・保存失敗の可視化・共有ハッシュの確認を実装
      ※ golf に金庫（GitHub自動保存）は無いので競合ガードは対象外。作るなら別項目

## P0で今回わかった追加（すべて修正済み・テストあり）
- [x] golf: rows の無いコンペ／members内のnull／`__proto__` という名のバッジ で画面が白のまま復帰不能
      （壊れたstateがlocalStorageに残るので再読み込みでも直らない）→ sanitize で除去
- [x] mleague: 1フィールド壊れているだけで isValidState が全体を弾き、全成績が初期状態に戻っていた
      → sanitize で直せる所は直してから判定するので、正しい成績は失わない
- [x] mleague: 共有リンク／ファイルの取り込みが即 push だった（street では既に直していた件の取り残し）
      → persist(true) にして、金庫へ出るのは記録係が次に自分で保存した時だけに
- [x] mleague: タブ復帰時の金庫取得が、この端末の未送信の編集を無言で捨てていた → dirty を見て確認する
- [x] mleague: 409 の盲目リトライを撤去。取得〜送信の隙に誰かが保存したら中止して通知
- [x] mleague: 金庫と中身が同じ時は PUT しない（空コミットを作らない）
- [x] mleague: results のキー数・件数に上限（1e6件で nextGameNum() が固まるのを防ぐ）

## P0の残り
- [ ] street の競合ガードだけ `t`（時計）基準のまま。mleague と同じ内容ハッシュ方式へ揃える
      （端末間の時刻ズレで他人の記録を消す余地が理屈上まだある）

## P1（実戦UX：審査員B）未着手
- [ ] 入力途中の値が未保存（タブ破棄で全損）→ ドラフト自動保存＋復元バナー
- [ ] streetに取り消しなし → 汎用Undoスタック（削除/修正/日削除/祝儀削除）
- [ ] 修正/削除が27pxで隣接 → 44px化・分離、「直前の第N戦を修正」を入力カードに常設
- [ ] 締め作業の入力欄が全部16px未満でiOS自動ズーム → 16px以上に
- [ ] ±ボタンでキーボードが落ちる → pointerdownのpreventDefault
- [ ] 保存ボタンがキーボード下に隠れる → 入力有効時に固定 or 複製
- [ ] どの日に記録中か入力カードに出ていない → 常時表示＋過去日は黄帯
- [ ] streetに閲覧モードなし（全員が削除可）→ is-admin/sync-banner移植、visibilitychange再取得
- [ ] picked/entryModeが非永続 → state保存

## P1（色・グラフ・ラボ：審査員A）未着手
- [ ] 直近5戦の1着色を廃し形（反転チップ）で／4着に点線枠
- [ ] ptバーを発散型（ゼロ基準線・左右）に（色を消しても正負が読める）
- [ ] データラボ「直接対決」だけ相手名が出ていない → 名前表示
- [ ] ラボ：pt系と順位系のpillが同形・緑の向きが逆転 → 形で分離、順位は記号/符号差分
- [ ] 印刷CSS（@media print）が3ファイル皆無 → 座席表・順位表を紙対応
- [ ] 9.5〜10.5pxの多用、--mutedが4.65:1 → 11px下限、--muted濃く
- [ ] テーマトグル未実装（data-theme用CSSは全部あるのにUIなし）→ ☀/☾ 1個

## P1（初見・プロダクト：審査員C/E）未着手
- [ ] 既定名「プレイヤー1〜4」→ 空＋プレースホルダ、入力カードから直接リネーム
- [ ] golfがスマホで横スクロール（.table-scroll欠落、@mediaがdarkのみ）
- [ ] リーグ→LPリンクが0本（逆は3本）／indexのLP導線をカード化
- [ ] scoringに⌂部屋リンクなし
- [ ] 平均着順・連対率・ラス率（率指標）を通算表に
- [ ] streetにポイント推移グラフ移植（通算にスパークライン）
- [ ] 結果カードの画像書き出し（SNS共有・説明ゼロで機能が伝わる）
- [ ] ご祝儀：selectをpickチップ化・入力カードに統合・半荘に紐付け・1対多（役満3人）

## 不採用（審査員E）
- 半荘中のリアルタイム点棒管理／ログイン・同期／全国ランキング／OCR：構造優位を捨てる・入力コスト増

## 採点（現状）
UI/カラー68 ・ 実戦UX66（リーグ74/街61）・ 初見72 ・ 品質—（XSS/データ破壊が主眼）・ 競合62
